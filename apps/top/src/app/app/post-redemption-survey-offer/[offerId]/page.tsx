import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Survey | TOP', // Update the title here
  description: 'Local+ Post Redemption Survey', // Update the description here
};
export default function Page() {
  return null;
}
