import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Wallet | TOP', // Update the title here
  description: 'Local+ Wallet', // Update the description here
};
export default function Page() {
  return null;
}
