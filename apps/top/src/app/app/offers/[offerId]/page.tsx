import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Home | TOP', // Update the title here
  description: 'Local+ Offers', // Update the description here
};
export default function Page() {
  return null;
}
